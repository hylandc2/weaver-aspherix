"""weaver.aspherix.operators — operator factories.

Docstring-only: concrete factories are imported by full dotted path
(`from weaver.aspherix.operators.build import build_case`) and re-exported from the
leaf `weaver.aspherix` package. Keeps package import side-effect-free.
"""
